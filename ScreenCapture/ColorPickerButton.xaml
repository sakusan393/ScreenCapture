<UserControl x:Class="ScreenCapture.ColorPickerButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:colorPicker="clr-namespace:ColorPicker;assembly=ColorPicker"
             x:Name="Root"
             Width="30"
             Height="30"
             MinWidth="24"
             MinHeight="24">
    <Grid>
        <ToggleButton x:Name="SwatchButton"
                      IsChecked="{Binding IsOpen, ElementName=PickerPopup, Mode=TwoWay}"
                      Cursor="Hand"
                      Padding="0"
                      BorderThickness="0"
                      Background="Transparent"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Stretch"
                      Focusable="False">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="ButtonBorder"
                            Background="Transparent"
                            BorderBrush="#FF707070"
                            BorderThickness="1"
                            SnapsToDevicePixels="True">
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="BorderBrush"
                                    Value="White"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="ButtonBorder"
                                    Property="BorderBrush"
                                    Value="Gold"/>
                            <Setter TargetName="ButtonBorder"
                                    Property="BorderThickness"
                                    Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder"
                                    Property="Opacity"
                                    Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
            <Grid>
                <Border>
                    <Border.Background>
                        <DrawingBrush TileMode="Tile"
                                      Viewport="0,0,8,8"
                                      ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <GeometryDrawing Brush="#FFD8D8D8"
                                                     Geometry="M0,0 H8 V8 H0 Z"/>
                                    <GeometryDrawing Brush="White"
                                                     Geometry="M0,0 H4 V4 H0 Z M4,4 H8 V8 H4 Z"/>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Border.Background>
                </Border>
                <Border x:Name="SwatchColor"/>
            </Grid>
        </ToggleButton>

        <Popup x:Name="PickerPopup"
               Placement="Bottom"
               PlacementTarget="{Binding ElementName=SwatchButton}"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Fade"
               Opened="PickerPopup_Opened"
               Closed="PickerPopup_Closed">
            <Border Margin="4"
                    Padding="8"
                    Background="#FFF7F7F7"
                    BorderBrush="#FF606060"
                    BorderThickness="1"
                    CornerRadius="4">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8"
                                      ShadowDepth="2"
                                      Opacity="0.45"/>
                </Border.Effect>
                <colorPicker:StandardColorPicker x:Name="Picker"
                                                 Width="320"
                                                 Height="360"
                                                 ShowAlpha="True"
                                                 ColorChanged="Picker_ColorChanged"
                                                 PreviewMouseLeftButtonDown="Picker_PreviewMouseLeftButtonDown"
                                                 PreviewKeyDown="Picker_PreviewKeyDown"/>
            </Border>
        </Popup>
    </Grid>
</UserControl>
